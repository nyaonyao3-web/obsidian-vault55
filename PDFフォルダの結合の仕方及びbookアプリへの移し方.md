Macをお持ちであれば、標準アプリの「プレビュー」を使うことで、複数のPDFフォルダやファイルを最も美しく、素早く1冊に結合できます。

結合したPDFをMacからiPadへ送れば、自動的にブックアプリへ同期されます。

---

## Macで複数のPDFを1冊に結合する手順

Macの「プレビュー」アプリを使用するのが最も確実です。

## 1. プレビューでベースとなるPDFを開く

1. 結合したいPDFのうち、最初のファイル（または最初のフォルダ内のPDF）をダブルクリックして開きます。
2. 画面上のメニューバーから「表示」＞「サムネール」を選択します（画面左側にページの縮小版が表示されます）。

## 2. 他のPDFをドラッグ＆ドロップで追加する

1. 追加したい別のPDFファイル（または別フォルダ内のPDF）を Finder で選択します。
2. 選択したファイルを、プレビュー画面左側の「サムネールバー」の末尾（一番下のページの下）へドラッグ＆ドロップします。
3. ページの順番を入れ替えたい場合は、サムネールを上下にドラッグして自由に並び替えられます。

## 3. 名前を付けて保存する

1. メニューバーの「ファイル」＞「書き出し...」または「PDFとして書き出し...」をクリックします。
2. わかりやすい本タイトルを名前につけて保存します。

---

## 結合したPDFをiPadの「ブック」へ送る方法

Macで作成した1冊のPDFをiPadへ移すには、以下の2つの方法がおすすめです。

## 方法A：AirDropで送る（一番早い）

1. Macで結合したPDFファイルを右クリック（または二本指タップ）します。
2. 「共有」＞「AirDrop」を選び、あなたのiPadを選択します。
3. iPad側に通知が出たら「ブック」アプリで開くを選択すれば完了です。

## 方法B：iCloudドライブで自動同期

1. Macの Finder で「iCloud Drive」を開きます。
2. その中にある「ブック」フォルダに、結合したPDFを入れます。
3. これだけで、iPadの「ブック」アプリ内にも自動でファイルが登場します。

---

もし手順の途中で「うまくページが追加できない」「MacのOSが古くてメニューが見当たらない」などがあれば、お気軽に教えてください。 MacのOSのバージョンを教えていただければ、より細かな画面表示に合わせてナビゲートします。
